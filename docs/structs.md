# YodaDemo.exe — struct registry (single source of truth)

Every struct is **traced to its allocation** so the size is correct, and defined **once** in the Ghidra
DB (no duplicates). When a function still decompiles with non-idiomatic casts, the fix is to model the
struct it touches — then re-verify the decompilation reads like human C++.

Legend: **TRACED** = size confirmed at an `operator_new`/alloc site; **INFERRED** = size bounded by
observed field accesses (not yet pinned to an alloc — a TODO).

| Struct | Size | Provenance | What / key fields |
|---|---|---|---|
| `Zone` | **0x848** | TRACED — `operator_new(0x848)` in `Dta_ReadZone` (0x426acf) → `Zone_Ctor` | 18×18 map. `flags@4`/`areaType@6`/`typeByte3@7` (a 4-byte zone-type dword), `activatedFlag@8` (0 until World::PlaceZoneObjectsMaybe stamps its objects into the grid on first entry), `width@0xc`/`height@0xe`, `tiles[972]@0x10`. **7 embedded MFC arrays** (each a 0x14 `CObArray`: `pVtable`/`m_pData`/`m_nSize`/`m_nMaxSize`/`m_nGrowBy`), modeled with **element-typed variants** for auto-typing: `objects@0x7a8`(`CObArray_ZoneObj`), `iactScripts@0x7bc`(`CObArray_IactScript`), `entities@0x7d0`(`CObArray_MapEntity`), and `cobArray4/5@0x7e4/0x7f8` + `genCandidateA/B@0x80c/0x820` (`CDWordArray`, worldgen candidate-spot scratch). Access via `.m_pData[i]` / `.m_nSize`. `tempVar@0x834`/`randVar@0x838`/`globalVar@0x844`, `zoneUnk846@0x846`. **Module match-ready (2026-07-05)** |
| `Tile` | **0x40c** | TRACED — `operator_new(0x40c)` in `Game_OnWalk`/`Game_MovePlayer`/`FUN_0041a030` → `Tile_Ctor` (0x404da0) | 32×32 graphic. `header@0`(4B, likely vtable), `pixels[0x400]@4` (BlitTile blits from `tile+4`; reconciles `flags@0x404` + the 0x40c size — was listed @0), `flags@0x404` (bit0 = transparent → BlitMasked), `name` CString@0x408 |
| `IactScript` | **0x30** | TRACED — `operator_new(0x30)` in `Dta_ParseActn`/`Iact_ReadScript` | `conditions@8`(CObArray)`/condCount@0xc`, `commands@0x1c/cmdCount@0x20`, `doneFlag@0x2c` |
| `IactCondition` | **0x1c** | TRACED — `operator_new(0x1c)` in `Iact_ReadScript` | `opcode@4` (`IactCondOp`) + `args[5]@8` |
| `IactCommand` | **0x20** | TRACED — `operator_new(0x20)` in `Iact_ReadScript` | `opcode@4` (`IactCmdOp`) + `args[5]@8` + `text` CString@0x1c |
| `MapZone` | **0x34** | TRACED — grid stride at `World+0x4b4` (100 records); a **2nd 100-record grid follows at `zones+100`** (`World::RestoreGridFromBackup` 0x421520 copies backup→active) | overview-grid cell (10×10 galaxy). `exists@0` (0xffff=empty). **7-short cell quest/item block @0x8-0x14** (init -1 by `ResetGameStateMaybe`): `cellQuestSlot0/1@0x8/0xa`, `cellItemA/B/C@0xc/e/10` (read by IACT `CheckCellItem(s)`), `cellQuestSlot5/6@0x12/14`. **Solved flags** (all set to 1 by CMD_MarkZoneSolved): `flagSolved@0x18`, `flagC@0x1c`, `flagA/B@0x20/24`, `flagD@0x28`; short@0x2c (unread). No DA equiv (engine-specific) |
| `Puzzle` | **0x2c** | TRACED — `operator_new(0x2c)` in `Dta_ParsePuz2` (0x422fd0) → `Puzzle_Read` (0x404480) → `World.puzzles@0xd0` | item-for-item quest node (PUZ2/IPUZ record, DA `ipuz_element`). `vftable@0`, **`nType@4`** (0=item transaction ↔ zone type 0x10, 1=trade ↔ 0xf, 2=goal-zone prize ↔ 0xa, 3=world mission goal — picked per planet from fixed puzzle-id sets in `World::WorldgenBuildZoneListsMaybe` 0x41eab0), `unk2@8`/`unk3@0xc`/`unk14@0x14` (parse-only, **no runtime reader** — DA ipuz unk2/unk3/unk4), `itemA@0x10` (quest item tile id; goal puzzle's → `World.startItem`; blitted as the won item in ShowWinMessage), `itemB@0x12` (2nd/goal item, Yoda-only → `World.startItem2Maybe`), `text1..5`@0x18–0x28 (CStrings: text1=NPC request — dialogue is `text1+sep+nextPuzzle.text3`; text2=thanks/completion (OnDragItem success, revisit); text3=want/hint, also shown when goal already solved; text4=win text on goal completion; text5=**never read** (DA "unused")) |
| `Canvas` | **0x43c** | TRACED — `operator_new(0x43c)` (`Settings_FUN_0041bb10`/`Dta_FUN_00426c40`) → `Canvas_Init` (0x407df0) → `World.canvas@0x3270` | DIBSection offscreen buffer. `hdc@0`, `hbitmap@0xc`, BITMAPINFOHEADER@0x10 (`width@0x14`,`height@0x18`,`bitCount@0x1e`), `palette[256]@0x38`, `pBits@0x438` (separate DIB alloc) |
| `CDC` | 0x10 | model-only (MFC) — only `m_hDC@4` matters | MFC device context; `Render_Blit`'s dest param is `CDC*` (`BitBlt(dest->m_hDC, …)`) |
| `Character` | **0x4c** | TRACED — `operator_new(0x4c)` in `Dta_ParseChar` (0x421e70) → `Character::Read` (0x4047a0) → `World.characters@0xc0` (`Character**`) | **IN-MEMORY layout (disasm-pinned 2026-07-05 from Ctor 0x404670 / InitMaybe 0x404750 / GetFrameTile 0x404850): `vftable@0` (0x44b160), `short frames[24]@0x4` (3 banks × 8 facing-dirs, -1=none; row idx: dx=1→dy+6, dx=-1→dy+3, dx=0→(dy!=-1); bank offsets 0/8/16 shorts; fallback frame = frames[1] = down), **`typeFlags@0x34`** (short, Read arg a = ICHR flags low word: 1=hero/friendly, 2=enemy, 4=weapon — DA `ICHR_FRIENDLY/ENEMY/IS_WEAPON`), `moveType@0x36` (ICHR flags high word = behavior; `CharMoveType` — GameView::Tick AI switch), **`weaponCharId@0x38`** (short, init -1; from CHWP `id_2` — index into `World.characters` of the weapon CHAR whose projectile frames this char fires, used by `GetProjectileTile` in Tick/DamageEntityAt/HitEntityAt), **`health@0x3a`** (short, init 1; from CHWP `health` — entity dies when `MapEntity.damageTaken >= health`, Zone::DamageEntityAt), `currentFrame@0x3c` (short, cached by GetFrameTile; stamped onto layer 1 each Tick), `damage@0x3e` (short, default 1; from CAUX; AddHealth(-damage), <0 = harmless), `unk40@0x40` (short, Read arg c = ICHR unk_4; **parse-only, no runtime reader**), `unk44@0x44` (int, Read arg d = ICHR unk_5; **parse-only, no runtime reader**), `unk48@0x48` (short, never initialized or read — tail padding of the 0x4c alloc).** `InitMaybe` renamed **`Character::Init`** (sole caller = Read; the post-parse initializer). The old `name[0x10]@8`/`flags@0x18`/`framesA@0x1c` layout was the FILE format (DA `ichr_data`) — the name is read to stack and NOT stored in-memory; do not use file offsets for in-memory reads. |
| `ChwpEntry` | 6 | DA `chwp_entry` (CHWP chunk) | weapon record attached to `characters[id]`: `id1`,`id2`,`health` |
| `CauxEntry` | 4 | DA `caux_entry` (CAUX chunk) | aux record: `id1`,`damage` |
| `MapEntity` | **0x64** | TRACED — `operator_new(0x64)` in `Iact_ReadZaux` (0x406270) → `MapEntity_Ctor` (0x404c80) | placed enemy/NPC (IZAX entry; runtime AI state lives here, driven by GameView::Tick 0x40b270). **From file** (Zone::ReadZaux): `charId@4` (izax entity_id; -1 = killed), `x@6`/`y@8` (live grid pos), `item@0x26` (loot tile id+1 dropped on death; 0 → drop the zone's first visible type-0 ZoneObj), `numItems@0x28`+`unk2a@0x2a` (izax num_items+unk3, read as one dword; drop only if nonzero), **`waypoints@0x40`** (int[8] = 4 patrol (x,y) int pairs — the izax trailing 32 B; moveType 10 walks to `waypoints[seqIdx+1]`). **Runtime**: `damageTaken@0xa` (dies when ≥ `Character.health`), `active@0xc` (1=alive/visible; IACT ShowEntity/HideEntity/All toggle it), `unk10@0x10` (moveType-6 scratch counter), **`bulletX/Y@0x12/0x14`** (projectile pos on layer 1: init from x/y when firing, advanced by bulletDX/DY; cleared on death — the old `homeX/homeY` guess was wrong), `aiStepCounter@0x16` (flips bRetreating), `unk18@0x18`/`unk20@0x20`/`unk2c@0x2c`/`unk60@0x60` (init 0, **no reader found**), **`bRetreating@0x1c`** (moveType-1 phase: 0=advance, 1=back off), `timer@0x24` (pause/cooldown ticks; also stun via Zone::HitEntityAt), **`wanderDir@0x30`** (dir code -1..2: -1=up 0=down 1=right 2=left; re-rolled with timer), **`bRefreshFrame@0x34`** (init 1; forces walk-frame recompute then clears), **`bulletDX/DY@0x38/0x3a`** (shot direction, → GetProjectileTile), **`bulletStep@0x3c`** (bullet travel counter, <4 = range; was `animFlag`), **`seqIdx@0x3e`** (waypoint idx -1..2 for patrol / anim frame idx 0..5 for moveType 12) |
| `World` | **0x33c0** | TRACED — `CDeskcppDoc` `CRuntimeClass.m_nObjectSize` @0x44c2b0 | the CDocument game doc (real MFC name `CDeskcppDoc`). `tileArray@0x84`(Tile**), `zoneObjects@0x98`(Zone**), `characters@0xc0`, `mapChangeReason@0x60` (1/2=X-Wing arrive/depart, 8=script; set by the TransitionZone* handlers), `currentZone@0x2c0`, `playerX/Y@0x2e20/24`, `cameraX/Y@0x3330/34`, `invArray@0xa8`(CObArray: `inventory`m_pData@0xac/`inventoryCount`m_nSize@0xb0), `currentWeapon@0x2e2c`, `nWeaponBox{L,T,R,B}@0x32a4/a8/ac/b0` (weapon-box screen rect), `nHealthDial{L,T,R,B}@0x32c4/c8/cc/d0` (health-circle screen rect), `nArrowBox{L,T,R,B}@0x32e4/e8/ec/f0` (direction-arrows panel rect), `nSoundEnabled@0x330c`/`nMusicEnabled@0x3310` (audio toggles → `World::ToggleSound/ToggleMusic`), `healthHi@0x3314`/`healthLo@0x3318`, **`pPlayerFrameTile@0x3358`** (Tile* — the player's current animation frame, computed from `pPlayerChar`+(nMoveDX,nMoveDY) in OnSetCursor/OnBumpTile/UpdatePlayerWalkFrame; blitted by `World::DrawPlayer` 0x41a6d0), **`pPlayerChar@0x335c`** (Character* = `characters[0]`, set at end of `World::ParseChar` — NOT a puzzle ptr as the old placeholder name guessed), score/experience (see game-logic.md). **Story/goal + session-flag fields (GameData CU sweep 2026-07-05):** **`nRequestedGoalItemMaybe@0x6c`** (was `bHasGeneratedWorldMaybe` — actually the goal-item/story id to build next, -1=none; set by `OnReplayStory`, gates the story-history load in `Generate`, reset -1 after generation), **`storyHistoryNevada/Alaska/Oregon@0x284/0x298/0x2ac`** (were `planetTable1/2/3Maybe` — `CWordArray`s of recently played story goal-item ids per planet 1/2/3 = desert/ice/forest, matching `AreaType`; persisted obfuscated in registry `[GameData] Nevada0..N` etc. by `World::Load/SaveStoryHistory*` 0x401ac0..0x402ff0; capped ~3; last element = File>Replay Story source), `nCurrentGoalItemMaybe@0x33a0` (goal item of the current story: Generate writes it — demo hardcodes 108 — Serialize restores from .wld), `bStartingGameMaybe@0x2e40` (1 during `StartGame`; read by LoadWorldMaybe), `bWorldInvalidMaybe@0x337c` (1=no valid world; polled by GameView OnWalk/MovePlayer/UpdateFrame), `bHidePlayerMaybe@0x2e54` (nonzero → DrawPlayer skips the player blit; toggled by IACT), `bWorldReadyMaybe@0x32f4`, `nCurrentAmmoMaybe@0x3354` (short; DEC'd by GameView::UseTile), `unk2e34/unk2e60/unk3378` (commented in DB). **`CWordArray` is now modeled for real (0x14: vftptr/m_pData/m_nSize/m_nMaxSize/m_nGrowBy)** — all 8 embedded word arrays (0x1e4..0x2ac) auto-expanded; array accesses now decompile as `.m_pData[i]`/`.m_nSize` |
| `GameView` | **0x310** | TRACED — `CDeskcppView` `CRuntimeClass.m_nObjectSize` @0x44b228 | the CView subclass (`CDeskcppView`). Coverage ~66% named (2026-07-05). Core: `pInvScrollBar@0x40`, `pWorld@0x44`(World*), `bBusy@0x4c` (frame-op guard; UpdateFrame skips if set), `bMouseCaptured@0x6c`, `frameCounter@0xb0`, `soundSession@0xc4`, `hCursor*@0xc8-0xf0`. **Drag state:** `bDragging@0x130`, `pDragZone@0x134`(Zone*, =currentZone during drag), `draggedTile@0x140`(Tile*), `nDragSlot@0x144` (inventory slot), `dragActive@0x148`, `nDragOrigin{X,Y}@0x14c/0x150`(Maybe), `nSavedCamera{X,Y}@0x170/0x174`, `nDragTile{X,Y}@0x94/0xc0`(Maybe). **Input/move:** `bMovePending@0x78`, `nMoveCommand@0x84`(0x21-0x28 dirs), `nMoveDX/DY@0x128/0x12c`, `bInInputHandler@0x11c`(Maybe). `nTargetZoneId@0x114` (pending transition), `strCheatBuffer@0x17c`(CString — accumulates typed cheats goyoda/gojedi). Remaining ~14 unnamed: cursor-state (0x7c/0x98/0x9c), 0xf4-0x100 block, 0x2e4/0x300-0x30c |
| `ZoneObj` | **0x10** | TRACED — `operator_new(0x10)` in `Dta_ParseHtsp` (0x4236b0) → `ZoneObj_Ctor` (0x404ed0)/`ZoneObj_Read` (0x404fe0) → `zone->objects@0x7ac` | placed hotspot (DA `obj_info`). **Corrected off-by-4:** `vtable@0`, `type@4` (`ObjType`, switched in reader), `x@8`, `y@0xa`, `arg@0xc` (type-dependent), `visible@0xe` (0xffff) |
| `CFile` | 0x40 | model-only (MFC) — only `vtbl@0` + `Read`@vtbl+0x3c matter for the match | MFC file; `CFile_vtbl.Read` is a fn-ptr |
| `TextDialog` | **0xc8** | INFERRED — stack-allocated in `GameView::ShowTextDialog`; fields bounded by its 4 methods (2026-07-05) | in-game speech-bubble dialog (SayText/ShowText box). `pText`(CString)@0xb8, `pParentView`(GameView*)@0xc0. Methods `TextDialog::Ctor`(0x416b90)/`DoModal`(0x416c40, CreateFont+GetMessage loop)/`PositionMaybe`(0x417570)/`Layout`(0x4176f0, RoundRect). Was mislabeled as GameView methods until modeled |
| `InvScrollBar` | **0x44** | INFERRED — CWnd-derived; fields bounded by `Inv/Add/RemoveItem` accesses (2026-07-05) | the inventory scroll bar (custom `CScrollBar` subclass), pointed to by `GameView.pInvScrollBar@0x40`. `m_hWnd@0x1c` (CWnd base), `scrollMax@0x3c` (=max(0,inventoryCount-7)), `scrollPos@0x40` (clamped [0,scrollMax]). `InvScrollBar_OnVScroll` (0x409360) = reflected WM_VSCROLL. See game-logic.md |

## Enums
- `IactCondOp` (`COND_*`, 0x00–0x23) — condition opcodes per `scrdoc.txt`; applied to `IactCondition.opcode`
- `IactCmdOp` (`CMD_*`, 0x00–0x25) — command opcodes per `iact.c` `commands[]`; applied to `IactCommand.opcode`
- `TileFlags` (`TILE_*`, base bits 0x1–0x100) — tile classification per DA `tile.h` (`GAME_OBJECT`/`WEAPON`/`ITEM`/`CHARACTER`/`PUSH_PULL`/…); applied to `Tile.flags`. BIT16+ sub-flags are type-dependent (weapon vs item vs door), left numeric
- `ObjType` (`OBJ_*`, 0–15) — hotspot type per DA `objectinfo.h`; applied to `ZoneObj.type` (`ZoneObj_Read` switches on it: QUEST_ITEM_SPOT/SPAWN/ITEM/WEAPON/DOOR_IN/OUT/LOCK/TELEPORTER/XWING/…)
- `MapFlags` (`MAP_*`, 1–18) — zone quest/map flag per DA `map.h` (`ENEMY_TERRITORY`/`ITEM_FOR_ITEM`/`INDOORS`=8/`FIND_THE_FORCE`/…); applied to `Zone.flags`
- `AreaType` (`AREA_*`: DESERT=1/SNOW=2/FOREST=3/SWAMP=5) — zone terrain per DA `map.h`; applied to `Zone.areaType`
- `MapChangeReason` (`MAPCHG_*`, 0–7) — map-transition trigger per DA `player.h` (DOOR_IN/OUT/VEHICLE/XWING/SCRIPT); ready to apply to the World map-change field when located

## Real MFC class names (from `CRuntimeClass`)
The app's own classes are `CDeskcpp*` ("Deskcpp" = Desktop Adventures C++): `CDeskcppDoc` (=`World`,
0x33c0), `CDeskcppView` (=`GameView`, 0x310). Descriptive names `World`/`GameView` are kept in the DB
for readability; the CRuntimeClass structs live at 0x44c2b0 / 0x44b228 and pin the sizes. To pin any
other MFC-derived class size, find its `CRuntimeClass {char* name, int nObjectSize, …}` in `.rdata`.

## Correction: `World.canvas` IS a fixed struct (2026-07-04)
Earlier I wrongly concluded the canvas was a computed-size buffer. Tracing `Render_Blit`'s `this`
argument → it's **`pWorld->canvas`** (@doc+0x3270), a fixed **`Canvas` = 0x43c** DIBSection wrapper
(`operator_new(0x43c)` in `Settings_FUN_0041bb10`/`Dta_FUN_00426c40` → `Canvas_Init` 0x407df0). The
pixels are a **separate** DIB allocation (`CreateDIBSection` fills `pBits@0x438`), not inline — which
is why the earlier alloc scan didn't see a giant block. Now modeled and typed (see registry).

## Open items
- `World+0x88` `tileCount`, the tile-array `CObArray`/`CPtrArray` object at doc+0x80 — confirm the class.
- Map the `Canvas` header fields (pixel base, W/H around +0x438) if the render path needs more clarity —
  but keep it a header-only model (pixels are variable-length inline).
