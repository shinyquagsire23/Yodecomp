// Top-level build file. AGP is applied (versioned) in app/build.gradle via the plugins block +
// the pluginManagement repos in settings.gradle.
plugins {
    id 'com.android.application' version '8.4.0' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
